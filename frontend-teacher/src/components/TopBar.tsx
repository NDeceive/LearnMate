import {LogOut,ShieldCheck} from 'lucide-react';import {useAuth} from '../auth/AuthContext';import {navigate} from '../router';
export function TopBar(){const{user,signOut}=useAuth();return<header className="sticky top-0 z-10 flex h-20 items-center justify-between border-b border-slate-200 bg-white/90 px-8 backdrop-blur"><div><div className="text-sm font-semibold text-slate-900">{user?.displayName}</div><div className="mt-1 flex items-center gap-1 text-xs text-slate-400"><ShieldCheck size={13}/>{user?.role}</div></div><button onClick={()=>{signOut();navigate('/login')}} className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-xs text-slate-600 hover:bg-slate-50"><LogOut size={15}/>退出登录</button></header>}
