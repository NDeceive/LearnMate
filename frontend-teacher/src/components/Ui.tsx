import type {ReactNode} from 'react';import {AlertCircle,Inbox} from 'lucide-react';
export function PageTitle({title,description,action}:{title:string;description:string;action?:ReactNode}){return<div className="mb-7 flex items-end justify-between"><div><h1 className="text-2xl font-bold tracking-tight text-slate-900">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p></div>{action}</div>}
export function State({loading,error,empty,children,onRetry}:{loading?:boolean;error?:string;empty?:boolean;children:ReactNode;onRetry?:()=>void}){if(loading)return<div className="rounded-2xl border bg-white p-12 text-center text-sm text-slate-500">正在读取真实数据…</div>;if(error)return<div className="rounded-2xl border border-rose-200 bg-rose-50 p-6 text-sm text-rose-700"><div><AlertCircle className="mr-2 inline" size={18}/>{error}</div><button type="button" onClick={onRetry||(()=>window.location.reload())} className="mt-4 rounded-lg border border-rose-300 bg-white px-4 py-2 font-medium hover:bg-rose-100">重试</button></div>;if(empty)return<div className="rounded-2xl border bg-white p-12 text-center text-sm text-slate-500"><Inbox className="mx-auto mb-3"/>暂无数据</div>;return children}
export function Metric({label,value,detail}:{label:string;value:string|number;detail?:string}){return<div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="text-xs font-medium text-slate-500">{label}</div><div className="mt-3 text-3xl font-bold text-slate-900">{value}</div>{detail&&<div className="mt-2 text-xs text-slate-400">{detail}</div>}</div>}
export function pct(value:number|null|undefined){return value===null||value===undefined?'暂无数据':`${Math.round(value*10)/10}%`}
export function fmt(value:string|null|undefined){return value?new Date(value).toLocaleString('zh-CN',{hour12:false}):'暂无记录'}
