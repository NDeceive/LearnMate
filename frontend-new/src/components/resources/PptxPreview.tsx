import React from "react";export default function PptxPreview({content}:{content:{slides?:Array<Record<string,unknown>>}}){return <div className="grid gap-3 md:grid-cols-2">{(content.slides||[]).map((s,i)=><div key={i} className="aspect-video rounded-xl border bg-gradient-to-br from-blue-50 to-white p-4 shadow-sm"><span className="text-[10px] text-slate-400">SLIDE {i+1}</span><h4 className="mt-2 font-bold text-blue-900">{String(s.title||"")}</h4><p className="mt-3 line-clamp-5 whitespace-pre-wrap text-xs text-slate-600">{Array.isArray(s.bullets)?s.bullets.join("\n"):String(s.body||s.subtitle||s.explanation||"")}</p></div>)}</div>}
