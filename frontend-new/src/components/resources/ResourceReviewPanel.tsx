import React from "react";export default function ResourceReviewPanel({review}:{review:{status:string;score:number;summary:string;issues:Array<Record<string,unknown>>;checks?:Array<{name:string;passed:boolean;detail:string}>}}){const approved=review.status==="approved";return <div className={`rounded-xl border p-4 ${approved?"border-emerald-100 bg-emerald-50":"border-amber-100 bg-amber-50"}`}><div className="flex justify-between"><strong className={approved?"text-emerald-800":"text-amber-800"}>ReviewAgent：{review.status}</strong><span className="font-bold">{review.score} 分</span></div><p className="mt-2 text-sm text-slate-600">{review.summary}</p>{review.checks&&review.checks.length>0&&<div className="mt-3 grid gap-2 md:grid-cols-2">{review.checks.map(check=><div key={check.name} className="rounded-lg bg-white/70 p-2 text-xs"><strong>{check.passed?"✓":"!"} {check.name}</strong><p className="mt-1 text-slate-500">{check.detail}</p></div>)}</div>}{review.issues.length>0&&<ul className="mt-2 list-disc pl-5 text-xs text-slate-600">{review.issues.map((i,index)=><li key={index}>{String(i.message||"")}</li>)}</ul>}</div>}
