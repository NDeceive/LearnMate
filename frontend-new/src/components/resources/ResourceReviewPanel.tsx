import React from "react";export default function ResourceReviewPanel({review}:{review:{status:string;score:number;summary:string;issues:Array<Record<string,unknown>>}}){return <div className="rounded-xl border border-emerald-100 bg-emerald-50 p-4"><div className="flex justify-between"><strong className="text-emerald-800">ReviewAgent：{review.status}</strong><span className="font-bold text-emerald-700">{review.score} 分</span></div><p className="mt-2 text-sm text-slate-600">{review.summary}</p>{review.issues.length>0&&<ul className="mt-2 list-disc pl-5 text-xs text-slate-600">{review.issues.map((i,index)=><li key={index}>{String(i.message||"")}</li>)}</ul>}</div>}
