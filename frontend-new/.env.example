# Browser-visible API prefix only. Keep /api for the Docker/Nginx same-origin proxy.
# Do not put server secrets in any VITE_* variable.
VITE_API_BASE_URL=/api

# Development-only proxy target used by Vite; it is not embedded as a secret.
VITE_DEV_API_PROXY_TARGET=http://localhost:5800
